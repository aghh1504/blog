<%include ./partials/header %>

 <div class="ui main text container segment">
     <div class="ui huge header">Add Photo <%= blog.title %></div>
     <form class="ui form" action="/blogs/<%= blog._id %>/add-photo?_method=PUT" method="POST">
        
        <div class="field">
             <label>Image</lable>
             <input type="text" name="blog[image]" value="<%= blog.image %>">
        </div>
        <div class="field">
             <label>Image</lable>
             <input type="text" name="blog[imageA]" value="<%= blog.imageA %>">
        </div>
        <div class="field">
             <label>Image</lable>
             <input type="text" name="blog[imageB]" value="<%= blog.imageB %>">
        </div>
        <div class="field">
             <label>Image</lable>
             <input type="text" name="blog[imageC]" value="<%= blog.imageC %>">
        </div>
        
             <input class="ui violet big basic button" type="submit">
         </form>
</div>

    
<%include ./partials/footer %>